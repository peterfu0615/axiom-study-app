# Axiom 错题本 — 产品需求文档（PRD）

> 版本：v0.1  
> 日期：2026-07-28  
> 平台：macOS 首发，iPhone 作为连续互通相机  
> 产品阶段：MVP 定义与技术可行性方案

## 1. 产品概述

Axiom 是一个面向中学生、大学低年级学生和自学者的智能错题工具。用户在 Mac 上直接调用 iPhone 摄像头拍摄试卷或作业，系统自动校正页面、切分题目、识别题干与手写答案，再由多模态模型生成可核验的标准答案、推导步骤、知识点和错因。错题会进入个人知识库，并按遗忘规律安排每日复习。

产品核心不是“拍照搜答案”，而是完成以下闭环：

**采集错题 → 理解错因 → 形成可编辑错题卡 → 按时复习 → 重新作答 → 验证掌握程度。**

### 1.1 一句话价值

把整理一道错题所需的时间从 5–10 分钟降低到约 30 秒，同时自动把错题变成每天可执行的复习计划。

### 1.2 产品原则

1. **先保真，再自动化**：原图永远保留；模型结果必须可编辑、可追溯。
2. **先让用户作答，再展示答案**：复习模式默认隐藏解析，防止工具退化为答案浏览器。
3. **置信度驱动交互**：高置信度自动完成，低置信度明确提示用户确认。
4. **题目与记忆状态解耦**：换模型、重跑解析或修改题干，不丢失复习历史。
5. **数学内容结构化**：公式使用 LaTeX；几何图使用 TikZ/SVG 等可渲染、可编辑表达。

## 2. 背景与机会

传统错题本的主要摩擦来自：

- 拍照后仍需手动裁图、分类和抄题；
- 搜题工具倾向于直接给答案，缺少对“为什么错”的建模；
- 错题积累后难以知道近期最薄弱的知识点；
- 用户不知道今天应该复习哪几道题；
- 几何题图片难以长期编辑、检索和复用；
- 模型答案可能有误，但多数产品没有明确的验证和纠错机制。

Axiom 的差异化在于将 macOS 的桌面学习场景、iPhone 的高质量相机、多模态推理和间隔复习放在同一个工作流中。

## 3. 目标与非目标

### 3.1 MVP 目标

- 用户无需安装配套 iOS App，即可在 Mac 中选择连续互通相机并拍照。
- 单页试卷可自动完成透视校正，并切分为多个候选题块。
- 用户可在保存前快速合并、拆分、调整或删除题块。
- 支持数学、物理、化学的印刷题干；手写内容作为辅助输入。
- 为每道错题生成结构化题干、标准答案、分步推导、知识点、难度和错因候选。
- 数学公式以 LaTeX 展示；常见平面几何图可生成 TikZ 或 SVG 预览。
- 提供“今日复习”，根据用户每次复习评分计算下一次日期。
- 提供近 7 天、30 天的错误知识点与错因统计。

### 3.2 暂不纳入 MVP

- Windows、Web、Android 客户端；
- 教师、班级、家长协作；
- 自动批改整套试卷并生成正式成绩；
- 对潦草手写过程做逐行可靠识别；
- 保证所有竞赛题、证明题均能得到正确答案；
- 复杂立体几何图的完全可编辑重建；
- 题库交易、社交排名和游戏化体系；
- 独立 iPhone App。

## 4. 目标用户

### 4.1 核心用户

每周有稳定作业或考试、使用 Mac 学习、希望长期整理错题的中学与大学低年级学生。

### 4.2 次级用户

- 备考研究生、职业资格考试的自学者；
- 为孩子协助整理错题的家长；
- 希望观察学生知识薄弱点的个人教师。

### 4.3 核心场景

1. 晚上订正作业时，连续拍摄 3–5 页并快速收录错题。
2. 考试后批量导入试卷，只保留做错或不确定的题。
3. 每天开始学习前完成 10–20 分钟“今日复习”。
4. 周末查看近期错误集中在哪些章节、题型和错因。
5. 考前按知识点、时间或掌握度生成专项练习。

## 5. 关键用户旅程

### 5.1 首次采集

1. 用户打开“添加错题”。
2. App 请求相机权限，并显示可用摄像头。
3. 用户选择 iPhone 连续互通相机。
4. 实时画面显示页面边界、清晰度、反光和倾斜提示。
5. 用户手动拍摄；稳定且清晰时可选择自动拍摄。
6. 系统完成页面校正并显示候选题块。
7. 用户通过拖拽边界、合并、拆分或删除完成确认。
8. 用户勾选“这道题我做错了”或录入自己的答案。
9. 系统后台解析；用户可继续拍下一页。
10. 解析完成后，用户确认题干、科目和答案并保存。

### 5.2 今日复习

1. 首页显示“今日待复习 12 题，预计 18 分钟”。
2. 进入题目后只显示题干和图，不显示答案。
3. 用户在纸上、脑中或 App 草稿区作答。
4. 点击“查看答案”，对照步骤和错因。
5. 用户选择“忘记 / 困难 / 掌握 / 轻松”。
6. 调度器更新该题记忆状态和下次复习日期。
7. 完成后展示本次正确率、薄弱知识点和明日预计负担。

### 5.3 纠正模型

1. 用户发现题干、图或解答有误。
2. 点击对应区块进入编辑，或选择“重新解析”。
3. 用户可框选原图并补充一句说明，例如“这里是角 ABC，不是角 ADC”。
4. 系统保存人工版本和模型版本，重跑时不覆盖用户确认内容。
5. 用户可标记“答案不可信”，该题不进入自动复习，直到被确认。

## 6. 功能需求

### 6.1 相机与采集

#### P0

- 使用 AVFoundation 枚举 Mac 内建相机、USB 相机及连续互通相机。
- 默认使用系统首选摄像头；允许用户手动切换并记住偏好。
- 支持实时预览、手动对焦提示、曝光提示和拍照。
- 拍摄完成后保留原始图片和经校正图片。
- 支持拖入图片、从剪贴板粘贴、从 Finder 导入，作为相机失败时的后备路径。
- 权限拒绝、iPhone 中断、相机被占用时给出明确恢复指引。

#### P1

- 稳定画面自动拍摄。
- 连续多页队列。
- 清晰度、阴影、反光、裁边不完整检测。
- 使用快捷键或音量键触发拍照。

### 6.2 页面检测与自动切题

处理分为两层：

1. **页面层**：检测纸张四角、透视校正、旋转、去阴影、增强对比度。
2. **题目层**：结合 OCR 坐标、题号模式、留白、分栏、横线和图形区域产生题块。

#### P0

- 检测单页主要文档边界并完成透视校正。
- 对中英文题号进行候选识别，如 `1.`、`（1）`、`12、`、`Q3`。
- 支持单栏和常见双栏试卷。
- 每个候选题块展示可拖动边界。
- 支持合并相邻题块、横向或纵向拆分、删除误检题块。
- 题图、题干、选项必须保留在同一题块内。
- 自动切题失败时允许用户直接框选。

#### 验收指标

- 清晰、正放、常见 A4 数学试卷中，题块召回率目标 ≥ 90%。
- 自动题块无需调整即可保存的比例目标 ≥ 75%。
- 不以像素级 IoU 作为唯一产品指标；以“是否遗漏题干或串入相邻题”为主。

### 6.3 OCR 与题目结构化

#### P0

- 端上 OCR 输出文本、边界框和置信度，用于切题与即时预览。
- 多模态模型直接接收题目原图，不能只依赖 OCR 文本。
- 结构化输出至少包含：
  - 科目；
  - 题型；
  - 题干 Markdown；
  - LaTeX 公式；
  - 选项；
  - 图形说明；
  - 标准答案；
  - 分步推导；
  - 知识点；
  - 难度；
  - 置信度；
  - 需要用户确认的问题。
- 公式渲染失败时回退显示原始 LaTeX 和题目截图。
- 所有字段可编辑，人工编辑后设置 `user_verified = true`。

#### 输出约束

- 推导中的每一步包含“结论”和“依据”。
- 不允许只给最终答案。
- 选择题要解释其他关键选项为何不成立。
- 证明题明确已知、待证、关键构造和结论。
- 模型无法确定时必须返回 `needs_review`，不得虚构缺失条件。

### 6.4 正解、错因与相似题

#### P0

- 系统根据题目和用户答案生成标准解法。
- 用户未提供原答案时，不声称知道其个人错因，只给出“可能错因”。
- 用户提供答案或手写过程后，输出：
  - 首个错误步骤；
  - 错误类型；
  - 正确替代步骤；
  - 建议复习知识点。
- 错因分类采用固定一级标签，并允许模型给出二级说明。

建议一级错因：

- 概念不清；
- 公式记错；
- 条件遗漏；
- 计算失误；
- 符号/单位错误；
- 图形理解错误；
- 推理跳步；
- 审题错误；
- 方法选择不当；
- 会做但时间不足；
- 模型无法判断。

#### P1

- 从已确认错题生成一道数值或情境变化的相似题。
- 生成题必须先由模型独立求解并进行一致性检查，失败则不展示。

### 6.5 几何图重建

“将几何题图片转为 LaTeX”应具体定义为：**从图片中提取几何语义，生成 TikZ 代码，并同时渲染 SVG/PNG 供界面展示。**

#### P0

- 识别常见点、线段、圆、角标、平行/垂直符号和文字标签。
- 模型先输出几何场景图，而不是直接自由生成 TikZ。
- 场景图至少包含：
  - 实体：点、线、射线、圆、多边形；
  - 约束：共线、平行、垂直、相等、相切、角度；
  - 标签及其相对位置；
  - 坐标仅作为绘图布局，不作为题目事实。
- 由确定性转换器将场景图编译为 TikZ。
- 原图与重建图可重叠对比；用户可切回原图。
- 低置信度或语义不完整时，默认使用原始裁图，不阻塞错题保存。

#### P1

- 可视化编辑点、线、标签和约束。
- 用户修改场景图后重新生成 TikZ。
- 支持输出独立 `.tex` 或 SVG。

#### 关键限制

- 图中的视觉比例通常不是数学条件，系统不得从“看起来相等”推断“长度相等”。
- 模糊手绘图、复杂辅助线和立体几何在早期版本中只保留原图。
- TikZ 重建属于增强功能，不应成为采集主流程的阻塞步骤。

### 6.6 错题库

#### P0

- 卡片和列表两种视图。
- 按科目、章节、知识点、题型、日期、掌握度、是否确认筛选。
- 全文搜索题干、标签和备注。
- 支持收藏、归档、删除和恢复。
- 支持查看原图、校正图、题块图和解析版本。
- 同一道题重复导入时提示可能重复，由用户决定合并或保留。

#### P1

- 相似错题聚类。
- 批量修改知识点。
- 导出为 Markdown、PDF 或 Anki 可导入格式。

### 6.7 今日复习与记忆调度

MVP 建议使用 FSRS 类调度模型，而不是写死“第 1、3、7、14 天”。每道题维护难度、稳定性和可回忆概率，调度目标是让预计回忆率维持在用户设定值附近。

#### P0

- 复习评分：忘记、困难、掌握、轻松。
- 调度输入：评分、作答耗时、上次复习时间、历史评分。
- 每次评分后立即计算 `next_review_at`。
- 默认目标保留率建议为 90%，高级设置可调整。
- 每日队列优先级：
  1. 已到期且预计遗忘概率高；
  2. 同一薄弱知识点的近期错题；
  3. 新增但未完成首次订正的题。
- 用户可设置每天最多题数或最长学习时间。
- 到期题过多时平滑分配，不能一次性制造不可完成的复习堆积。
- 复习中默认隐藏答案与个人上次答案。
- 支持撤销刚才的评分。

#### 初始冷启动

新题在完成订正后进入首次复习。用户第一次评价后建立初始记忆状态；在累计足够个人数据前使用全局默认参数，之后再逐步个性化。

### 6.8 学习统计

#### P0

- 首页展示：
  - 今日待复习数；
  - 今日预计用时；
  - 连续学习天数；
  - 近 7 天新增错题数；
  - 近 7 天复习完成率。
- 洞察页展示：
  - 近 7/30 天按知识点的错题数量；
  - 各知识点错误率和复习后掌握度；
  - 错因分布；
  - 重复出错的知识点；
  - 科目趋势；
  - 到期复习负担预测。
- 至少有 3 道题才展示某知识点趋势，避免样本过小造成误导。
- 明确区分“新增错题数”和“复习时再次答错数”。

#### P1

- 自动生成每周学习报告。
- 展示“高频但已掌握”和“低频但持续遗忘”两类知识点。
- 支持按教材章节或考试范围查看薄弱点。

## 7. 信息架构

建议使用五个一级入口：

1. **今日**：待复习、未完成订正、今日进度；
2. **采集**：相机、图片导入、待处理队列；
3. **错题库**：搜索、筛选、题目详情；
4. **洞察**：知识点、错因和趋势；
5. **设置**：科目、教材体系、模型、隐私、复习负担。

题目详情页分为：

- 题目；
- 我的答案；
- 正确解法；
- 错因；
- 知识点；
- 复习记录；
- 原始材料与版本。

## 8. 技术方案

### 8.1 总体架构

```text
iPhone 连续互通相机 / 本地图片
                ↓
macOS 采集层（AVFoundation）
                ↓
本地预处理（Vision + Core Image）
  页面检测 → 透视校正 → OCR → 候选切题
                ↓
用户确认题块
                ↓
任务队列（可取消、重试、去重）
                ↓
多模态解析服务
  题目结构化 → 解答 → 知识点 → 错因 → 几何场景图
                ↓
本地数据库 + 图片文件存储
                ↓
复习调度器 / 统计聚合器 / 搜索索引
```

### 8.2 macOS 客户端

建议技术栈：

- Swift + SwiftUI；
- AVFoundation：预览和照片捕获；
- Vision：文档边界、OCR、文本框和轮廓辅助；
- Core Image：透视校正、裁切、对比度与降噪；
- SwiftData 或 Core Data：结构化数据；
- App Sandbox + Keychain：权限与密钥；
- BackgroundTasks/应用内队列：解析任务重试；
- KaTeX/MathJax 的 WKWebView 封装，或原生 LaTeX 渲染组件；
- TikZ 在服务端受限沙箱编译，客户端只接收 SVG；若完全本地化，则使用自研场景图到 SVG 渲染，避免在客户端执行任意 TeX。

连续互通相机在 macOS 13 起可作为 `AVCaptureDevice` 使用；设备发现应包含外部设备，并优先响应系统首选摄像头变化。产品上仍应保留 AppKit 的“一次拍照/扫描后导入”或普通图片导入作为后备方案。

### 8.3 自动切题算法

不建议第一版训练专用目标检测模型。先采用可解释的混合方法：

1. Vision 检测文档四边；
2. Core Image 完成透视校正；
3. OCR 获取每行文本、边界框和置信度；
4. 规则识别题号、分栏和段落；
5. 通过垂直留白、文字块邻接关系合并题干；
6. 通过轮廓、低文本密度区域识别配图；
7. 将配图附着到最近题干；
8. 输出候选框和置信度；
9. 收集用户调整记录，作为后续训练切题模型的标注数据。

当真实用户数据达到约 5,000–10,000 个已确认页面且覆盖足够版式后，再评估训练专用版面分析模型。

### 8.4 多模态解析策略

采用两阶段而不是“一次调用生成全部内容”：

#### 阶段 A：题目理解

- 输入：原始题块图、OCR 文本、用户补充；
- 输出：严格 JSON；
- 目标：题干、公式、选项、图形实体、条件、科目和知识点；
- 如果题目缺失，返回缺失项，不进入求解。

#### 阶段 B：求解与校验

- 输入：阶段 A 的结构化题目和原图；
- 第一次调用生成解法；
- 第二次以“审校者”角色独立检查条件使用、计算和最终答案；
- 不一致时尝试一次修订；仍不一致则标记需要人工确认。

对于高风险场景，界面应显示“AI 解析可能有误”，并提供原图与反馈入口。模型置信度不是正确率证明，必须结合结构校验、双路答案一致性和用户反馈。

### 8.5 建议的模型输出 Schema

```json
{
  "subject": "math",
  "problem_type": "geometry_proof",
  "stem_markdown": "如图，已知……",
  "latex_fragments": ["AB \\parallel CD"],
  "choices": [],
  "given_facts": [
    {"type": "parallel", "entities": ["AB", "CD"], "source": "text"}
  ],
  "question_targets": ["证明 ∠A = ∠C"],
  "solution": {
    "final_answer": "……",
    "steps": [
      {
        "conclusion": "∠A = ∠B",
        "justification": "两直线平行，内错角相等"
      }
    ]
  },
  "knowledge_points": [
    {"name": "平行线性质", "confidence": 0.95}
  ],
  "difficulty": 3,
  "geometry_scene": {
    "entities": [],
    "constraints": [],
    "labels": []
  },
  "quality": {
    "confidence": 0.82,
    "needs_review": false,
    "missing_information": [],
    "warnings": []
  }
}
```

### 8.6 本地与云端边界

#### 本地完成

- 相机预览与拍照；
- 页面校正；
- OCR 与候选切题；
- 用户编辑；
- 数据库存储；
- 复习调度；
- 统计与全文搜索。

#### 云端完成

- 多模态题目理解；
- 复杂公式转写；
- 正解和推导；
- 错因分析；
- 几何场景图生成；
- 可选的二次答案审校。

这样可保证没有网络时仍能拍摄、裁题和复习已保存内容；解析任务待联网后继续。

## 9. 数据模型

### 9.1 核心实体

#### `SourceDocument`

- `id`
- `original_image_path`
- `corrected_image_path`
- `captured_at`
- `source_type`
- `page_detection`
- `processing_status`

#### `Problem`

- `id`
- `source_document_id`
- `crop_rect`
- `crop_image_path`
- `subject`
- `problem_type`
- `stem_markdown`
- `structured_content_json`
- `solution_json`
- `model_confidence`
- `verification_status`
- `created_at`
- `updated_at`
- `archived_at`

#### `UserAttempt`

- `id`
- `problem_id`
- `answer_text`
- `answer_image_path`
- `is_correct`
- `first_error_step`
- `error_category`
- `duration_seconds`
- `created_at`

#### `KnowledgePoint`

- `id`
- `canonical_name`
- `parent_id`
- `subject`
- `curriculum_version`

#### `ProblemKnowledgePoint`

- `problem_id`
- `knowledge_point_id`
- `confidence`
- `source`：模型或用户

#### `ReviewState`

- `problem_id`
- `difficulty`
- `stability`
- `retrievability`
- `desired_retention`
- `last_review_at`
- `next_review_at`
- `review_count`
- `lapse_count`

#### `ReviewLog`

- `id`
- `problem_id`
- `rating`
- `scheduled_days`
- `elapsed_days`
- `duration_seconds`
- `reviewed_at`
- `scheduler_version`

#### `ModelRun`

- `id`
- `problem_id`
- `provider`
- `model`
- `prompt_version`
- `schema_version`
- `input_hash`
- `output_json`
- `latency_ms`
- `token_usage`
- `status`
- `created_at`

### 9.2 数据设计要求

- 图片文件与数据库记录分开存储，数据库仅保存受控路径和哈希。
- 原始图不可被模型结果覆盖。
- 用户确认字段优先于模型重跑结果。
- 所有调度日志追加写入，便于重算和迁移算法。
- 知识点使用受控词表，模型自由文本先映射后入库。

## 10. 非功能需求

### 10.1 性能

- 相机预览保持流畅，目标 30 fps。
- 拍照后 1 秒内显示页面校正预览。
- 常见单页候选切题在 2 秒内完成。
- 题目进入解析队列后立即允许继续拍摄。
- 单题首个结构化结果目标在 15 秒内返回；超过 30 秒提示仍在处理。
- 1 万道错题下，搜索和筛选首屏响应目标 < 300 ms。

### 10.2 可靠性

- 解析任务支持断点、取消和指数退避重试。
- 同一图片与题块使用内容哈希避免重复计费。
- 数据库迁移必须保留 ReviewLog。
- 云端失败不影响本地浏览和复习。

### 10.3 隐私与安全

- 首次上传前明确说明题目图片会发送至模型服务。
- 默认裁切后再上传，避免发送整页中的姓名、学校和考号。
- 提供自动检测并遮盖姓名、学号等区域的选项。
- 支持“一键删除所有云端任务记录”和本地数据导出/删除。
- API 密钥不得硬编码在客户端；商业版本通过自有后端签发短期令牌。
- TikZ/TeX 编译必须在无网络、限时、限内存、禁止 shell escape 的沙箱中运行。
- 遥测默认不包含题目正文和原始图片。

### 10.4 可访问性

- 支持键盘完成采集确认和复习评分。
- 支持 VoiceOver 标签和动态字体。
- 颜色不作为置信度或正确性的唯一表达方式。

## 11. 成功指标

### 11.1 北极星指标

**每周完成有效复习的已确认错题数。**

“有效复习”定义为：用户先尝试作答、查看答案并提交评分，而不是仅浏览题目。

### 11.2 激活指标

- 新用户首日成功保存 ≥ 3 道错题；
- 首次采集到第一道保存完成的中位时间 < 3 分钟；
- 首周至少完成 2 天复习。

### 11.3 质量指标

- 自动切题直接接受率；
- 题干人工修改率；
- 解答被标记错误的比例；
- 低置信度题目被正确拦截的比例；
- 几何重建采用率和回退原图率；
- 重复导入识别准确率。

### 11.4 留存指标

- D7、D30 留存；
- 每周复习完成率；
- 用户连续四周仍在新增且复习错题的比例；
- 到期题积压中位数。

## 12. MVP 验收标准

满足以下条件即可进入小范围测试：

1. 在支持连续互通相机的 Mac/iPhone 环境中，可稳定发现设备、预览并拍摄（Apple的系统连续互通功能）
2. 支持导入图片作为完整后备路径。
3. 单页可透视校正并产生可编辑题块。
4. 题块可完成结构化解析，公式能正确渲染或安全回退。
5. 解析结果支持人工编辑、确认和重新解析。
6. 每道题可记录知识点、错因和至少一次用户作答。
7. 今日复习能根据四档评分更新下次日期。
8. 近 7/30 天知识点和错因统计正确。
9. 无网时可采集、浏览和复习，恢复网络后可继续解析。
10. 删除错题时，关联图片、索引和调度状态处理一致，并有恢复机制。

## 13. 开发分期

### 阶段 0：技术验证，1–2 周

- 连续互通相机发现、预览和高分辨率拍照；
- 页面检测与透视校正；
- 对 50–100 张真实试卷测试 OCR 和切题规则；
- 多模态 JSON Schema 稳定性验证；
- 一类平面几何题的场景图 → SVG/TikZ 验证。

**退出条件**：相机链路稳定；常见试卷可生成可调整题块；模型能稳定返回可解析 JSON。

### 阶段 1：采集与错题库，3–4 周

- 相机、导入、处理队列；
- 页面校正、候选切题、手动调整；
- 本地数据模型、搜索和题目详情；
- 原图/校正图/题块图版本管理。

### 阶段 2：AI 解析与纠错，3–4 周

- 结构化题目理解；
- 解答、审校、知识点和错因；
- LaTeX 渲染；
- 低置信度与人工确认流程；
- 模型调用观测、缓存和成本控制。

### 阶段 3：复习与洞察，2–3 周

- FSRS 类调度器；
- 今日复习；
- 近 7/30 天统计；
- 到期负担平滑；
- 数据导出和基础隐私设置。

### 阶段 4：几何增强与 Beta，2–4 周

- 场景图、TikZ/SVG 生成与回退；
- 重复题检测；
- 性能、崩溃、无网与边界场景测试；
- 20–50 名用户封闭测试。

单人全职开发的现实 MVP 周期约为 12–16 周；2 名客户端/后端工程师加 1 名兼职设计或算法工程师，可压缩到约 8–12 周。几何图编辑器应独立排期，避免拖延核心闭环。

## 14. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 连续互通相机在设备、系统版本和网络状态下不稳定 | 无法采集 | 图片导入、一拍即传和 USB 相机后备；保留相机诊断页 |
| 自动切题跨版式泛化不足 | 整理成本仍高 | 规则 + OCR 混合；优先做好极快的人工调整；收集修正标注 |
| 模型给出错误答案但语气确定 | 学习伤害 | 二次审校、结构校验、置信度门槛、原图对照和用户确认 |
| 手写公式识别差 | 错因判断失败 | 将手写答案设为可选；允许文本补充；先支持印刷题 |
| TikZ 重建“看起来对但条件错” | 几何推理被误导 | 语义场景图与视觉坐标分离；低置信度回退原图 |
| 每题多次模型调用导致成本高 | 商业不可持续 | 图片裁切、缓存、按难度选择模型、批处理、仅对需要的题二审 |
| 复习积压打击用户 | 留存下降 | 每日上限、负担预测、逾期平滑和“快速复习”模式 |
| 知识点标签不统一 | 统计失真 | 按教材维护受控词表；模型标签必须映射到标准节点 |

## 15. 待验证问题

1. 用户更偏好实时取景拍摄，还是系统原生“一次拍照/扫描”交互？
2. 用户会不会录入自己的错误答案？若不会，错因功能应如何降级？
3. 用户对解答正确性的容忍度和验证习惯如何？
4. 一页中平均有多少道需要保留的错题，是否应先切整页再选择？
5. 日常复习是“重新完整做题”，还是“口述思路/看关键步骤”？
6. 几何图可编辑重建是核心付费点，还是低频锦上添花？
7. 用户是否需要教材版本和年级体系，还是自由标签已经足够？
8. 用户愿意为云端解析订阅，还是更希望自带 API Key？

## 16. 建议的第一轮用户测试

招募 8–12 名真实目标用户，每人带 3 份近期作业或试卷，完成以下任务：

1. 不经指导添加一页错题；
2. 修正一次错误切题；
3. 判断一份 AI 解答是否可信；
4. 完成 5 道今日复习；
5. 解释洞察页告诉了自己什么。

重点记录：

- 从拍摄到保存的时间；
- 每页人工操作次数；
- 用户是否注意到低置信度警告；
- 用户是否愿意先作答再看答案；
- 哪个统计能促使其采取下一步行动；
- 用户对原图、重建图和 TikZ 的真实需求强度。

## 17. 技术依据

- Apple 的 AVFoundation 支持在 macOS 中把 iPhone 连续互通相机作为捕获设备使用，并提供系统首选相机机制：[Supporting Continuity Camera in your macOS app](https://developer.apple.com/documentation/avfoundation/supporting-continuity-camera-in-your-macos-app)。
- Apple Vision 可返回文档四角与分割结果，用于页面检测：[DetectDocumentSegmentationRequest](https://developer.apple.com/documentation/vision/detectdocumentsegmentationrequest)。
- Vision 的文字识别会返回文本候选、位置和置信度，可用于本地 OCR 与版面切分：[RecognizeTextRequest](https://developer.apple.com/documentation/vision/recognizetextrequest)。
- Core Image 提供基于四角坐标的透视校正：[CIPerspectiveCorrection](https://developer.apple.com/documentation/coreimage/ciperspectivecorrection)。
- 复习调度可参考开放的 FSRS 实现与 DSR 记忆模型：[Free Spaced Repetition Scheduler](https://github.com/open-spaced-repetition/free-spaced-repetition-scheduler)。

